# PostgreSQL connection for integration tests
POSTGRES_CONNECTION=Host=localhost;Port=5432;Database=managed_views_test;Username=test;Password=test

# Docker is required for Testcontainers-based integration tests
# Ensure Docker Desktop or Docker Engine is running before executing integration tests
